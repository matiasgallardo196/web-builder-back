# HTTP Server Port
# If omitted, uses: 3001
# Uncomment and adjust if you need another port:
# PORT=3001

# Allowed origin for CORS
# Examples:
# CORS_ORIGIN=http://localhost:3000
# CORS_ORIGIN=https://my-frontend.com
# CORS_ORIGIN=* (allows all origins)
# Recommended for development: * (allows all)
CORS_ORIGIN=*

# Database URL (if applicable)
# PostgreSQL example: postgresql://user:password@localhost:5432/mydb
# MySQL example: mysql://user:password@localhost:3306/mydb
# Optional: you only need this if you're going to use a database
DATABASE_URL=postgresql://user:password@localhost:5432/mydb

# Frontend URL (if you have one)
# Optional: only if you need this variable in your code
FRONTEND_URL=http://localhost:3000

# Logging
# Valid levels: fatal | error | warn | info | debug | trace | silent
# If omitted, uses: 'debug' in development, 'info' in production
# Uncomment and adjust as needed:
# LOGGER_LEVEL=debug

# If FULL_LOGS=true → logs complete request/response
# If FULL_LOGS=false → logs only basic data (method, url, statusCode)
# If omitted, uses: false (only basic data)
# Uncomment and adjust if you need complete logs:
# FULL_LOGS=false

# Rate limiting (Throttler)
# Time window in seconds
# If omitted, uses: 60 seconds
# Uncomment and adjust as needed:
# RATE_LIMIT_TTL=60

# Requests allowed per IP within the time window
# If omitted, uses: 100 requests
# You can increase this value in development if necessary
# Uncomment and adjust as needed:
# RATE_LIMIT_LIMIT=100
