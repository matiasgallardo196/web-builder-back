# HTTP Server Port
# In production, it's usually defined by the server environment
# If omitted, uses: 3000
# Recommended: let the server environment define it (don't define it here)
# PORT=3000

# Allowed origin for CORS
# ⚠️ IMPORTANT: Don't use * in production for security
# Define specific frontend URLs
# Example:
# CORS_ORIGIN=https://my-frontend.com
# For multiple origins, separate by commas
# CORS_ORIGIN=https://app1.com,https://app2.com
# REQUIRED in production
CORS_ORIGIN=https://your-frontend.com

# Database URL
# In production, use a secure connection and strong credentials
# REQUIRED if you use a database
DATABASE_URL=postgresql://user:password@localhost:5432/production_db

# Frontend URL (if applicable)
# Optional: only if you need this variable in your code
FRONTEND_URL=https://your-frontend.com

# Logging
# Valid levels: fatal | error | warn | info | debug | trace | silent
# If omitted, uses: 'info' in production, 'debug' in development
# Recommended for production: warn or info
# Uncomment and adjust as needed:
# LOGGER_LEVEL=warn

# If FULL_LOGS=true → logs complete request/response
# If FULL_LOGS=false → logs only basic data (method, url, statusCode)
# If omitted, uses: false (only basic data)
# ⚠️ In production, keep it at false for performance and security
# Uncomment only if you really need complete logs:
# FULL_LOGS=false

# Rate limiting (Throttler)
# Time window in seconds
# If omitted, uses: 60 seconds
# Adjust according to your expected load
# Uncomment and adjust as needed:
# RATE_LIMIT_TTL=60

# Requests allowed per IP within the time window
# If omitted, uses: 100 requests
# Adjust according to your expected traffic
# Recommended values: 100-1000 depending on the case
# Uncomment and adjust as needed:
# RATE_LIMIT_LIMIT=100
